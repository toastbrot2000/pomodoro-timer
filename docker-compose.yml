services:
  pomodoro:
    build: .
    image: pomodoro-timer
    container_name: pomodoro-timer
    ports:
      - "8080:8080"
    volumes:
      # named volume keeps the SQLite DB across container rebuilds/redeploys
      - pomodoro-data:/data
    restart: unless-stopped

volumes:
  pomodoro-data:
